package com.project2.goal.data;

import com.project2.goal.models.Goal;

import java.util.List;

public interface GoalDao {

    // don't need these - using Jpa built-in methods instead
//  List<Goal> findAll();
//	List<Goal> findGoalsByUserId(int userId);
//    int save(Goal goal);
//    Goal findOne(int goalid);
//    void delete( int goalId);
}
